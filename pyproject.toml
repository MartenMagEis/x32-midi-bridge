[project]
name = "x32-midi-bridge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.1",
    "python-osc>=1.10.2",
    "pymidi>=0.5.0",
    "zeroconf>=0.150.0",
    "python-rtmidi>=1.5.8",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
